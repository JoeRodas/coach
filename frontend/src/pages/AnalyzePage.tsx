// Scaffold stub — real analyze flow (PGN input, validation, loading/error/
// empty states, grounded-analysis render with expandable Sources + eval-drop
// chart) lands in PR-2.

export default function AnalyzePage() {
  return (
    <section aria-labelledby="analyze-heading" className="space-y-4">
      <h1 id="analyze-heading" className="text-2xl font-semibold tracking-tight">
        Analyze a game
      </h1>
      <p className="text-slate-600">
        The full analyze flow lands in the next PR. This page is a scaffold placeholder; the Phase 1
        experience remains live at the root domain.
      </p>
    </section>
  );
}
