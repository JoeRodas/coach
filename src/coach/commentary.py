"""Phase 1 generates the final explanation inside the agent loop (see `agent.py`).

This module is reserved for phases where commentary becomes a separate step with
its own prompt template, retry/validation logic, or structured-output schema.
Left intentionally empty until that's needed.
"""
